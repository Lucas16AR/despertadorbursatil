# Acceso server-side a Supabase. La service_role key NUNCA se expone al browser:
# sólo se usa en componentes de servidor / route handlers.
# Copiar de: dashboard de Supabase → Project Settings → API.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
